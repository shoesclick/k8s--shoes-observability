apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: swapispringboot
  namespace: apiexemplo
spec:
  hosts:
  - "*"
  gateways:
  - eks-dev-gateway
  http:
  - match:
    - uri:
        prefix: /sw-springboot/
    route:
    - destination:
        host: svc-swapispringboot.apiexemplo.svc.cluster.local
        port:
          number: 5000        
